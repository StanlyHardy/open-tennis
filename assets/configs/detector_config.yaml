model:
  execution_env: "cuda:0" # Supports CUDA, CPU , TensorRT (on compatible hardware)
  conf_thresh : 0.6
  iou_thresh : 0.5
  warm_up : 25
  class_labels : ["scoreboard"]
  stride: 32
  img_size: 640